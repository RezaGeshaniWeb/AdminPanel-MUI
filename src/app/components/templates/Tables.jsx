import { Box, Typography } from "@mui/material";

export default function Tables() {
    return (
        <Box
            sx={{
                py: 4,
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                textAlign: 'center',
            }}
        >
            <Typography>Dashboard content for Tables</Typography>
        </Box>
    )
}